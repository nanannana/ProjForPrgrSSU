extern struct Daeho* struuuct;
/// 전역변수로 구조체 포인터를 선언합니다.
/// 이 헤더를 가져다 쓰면 언제나 구조체에 접근 가능합니다.
/// 전역변수로 선언함으로써 매개변수로 번거롭지 않게 다음과 같은 함수들
/// fileIO.c 에서 읽기, 쓰기
/// manageData.c에서 정렬 등을 간편하게 사용 가능합니다.

void SortData(struct *..
